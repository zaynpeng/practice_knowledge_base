# 测试说明

## 基础启动

```bat
start.bat
```

访问 `http://127.0.0.1:5000`。

## 自动冒烟测试

```bat
.venv\Scripts\python.exe smoke_test.py
```

冒烟测试使用临时 SQLite 数据库，不会写入正式 `data/knowledge.db`。

## 建议验收流程

1. 在“问题地图”创建一个问题。
2. 在“快速收藏”粘贴链接，填写“我的感触”和“为什么收藏”，关联刚创建的问题。
3. 勾选“明确准备实践”，保存后检查是否生成实践记录。
4. 在实践详情填写最小测试、成功标准、实际结果、最终决定、可复用结论。
5. 将实践状态改为“已验证”，确认未填关键字段时会被阻止。
6. 导出实践 Markdown，检查 `exports/markdown/实践记录/`。
7. 在“书架”导入 CSV 或 Excel，确认重复书籍不会覆盖。
8. 记录读书感悟并勾选转为实践。
9. 在“导出与备份”执行数据库备份、JSON 导出、CSV 导出。

## 书籍 CSV 示例

```csv
书名,作者,ISBN,是否读过,当前兴趣,备注
影响力,罗伯特·西奥迪尼,,否,近期可读,报价和说服相关
```

## 链接异常测试

可以输入明显错误的链接，例如 `https://invalid.invalid/example`。系统应显示读取失败，但仍保存用户填写的个人内容。
